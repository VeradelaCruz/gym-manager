spring:
  application:
    name: trainer-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/gymdb

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8084

logging:
  level:
    root: INFO
