server:
  port: 8888

spring:
  application:
    name: gym-config-server

  cloud:
    config:
      server:
        git:
          uri: C:\Users\maida\OneDrive\Escritorio\Proyectos\gym-manager\gym-config-repo
          default-label: main
          search-paths: .


