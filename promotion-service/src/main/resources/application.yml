spring:
  application:
    name: promotion-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/gymdb

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8087

logging:
  level:
    root: INFO
